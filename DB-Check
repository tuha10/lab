Powershell: Test-NetConnection 172.16.212.11 -Port 1433 check port
netstat -ano | findstr :1433 - checkport chính mình or telnet localhost 1433
netstat -ano | findstr LISTENING | findstr sqlservr - check đang listen port nào

SELECT name, is_auto_close_on
FROM sys.databases
WHERE name LIKE 'ePO%';

ALTER DATABASE [ePO_DCSRV-EPO] SET AUTO_CLOSE OFF;
ALTER DATABASE [ePO_DCSRV-EPO_Events] SET AUTO_CLOSE OFF;

SELECT name, is_auto_close_on
FROM sys.databases
WHERE name IN ('ePO_DCSRV-EPO','ePO_DCSRV-EPO_Events');

SELECT @@SERVERNAME AS ServerName, @@SERVICENAME AS ServiceName;
SELECT name FROM sys.databases ORDER BY name;


-- Collation của SQL Server (instance)
SELECT SERVERPROPERTY('Collation') AS SQLServerCollation;

-- Collation của ePO Core và ePO Events DB
SELECT name, collation_name
FROM sys.databases
WHERE name LIKE 'ePO%';
